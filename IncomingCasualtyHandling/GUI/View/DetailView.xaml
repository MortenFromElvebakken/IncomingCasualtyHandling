<UserControl x:Class="IncomingCasualtyHandling.GUI.View.DetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IncomingCasualtyHandling.GUI.View"
             xmlns:viewModels="clr-namespace:IncomingCasualtyHandling.GUI.ViewModels"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition></RowDefinition>
            <!--<RowDefinition Height="50px"/>-->
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <xctk:IconButton Padding="5" Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext.ChangeViewCommand}" >
                    <xctk:IconButton.Icon>
                        <Image Source="{Binding IconPath}"></Image>
                    </xctk:IconButton.Icon>
                </xctk:IconButton>
            </Grid>
        </Grid>

        <!--<Grid Grid.Row="0" RowSpan="2" ZIndex="1">-->
        <Grid Grid.Row="1" ZIndex="1">
            <!--<TabControl ItemsSource="{Binding HomeTabTest}">
                <TabItem>
                    <TabItem.Header>
                        <Image Width="20" Source="{Binding IconPath}"></Image>
                    </TabItem.Header>
                </TabItem>
            </TabControl>-->
            <TabControl  ItemsSource="{Binding Tabs}" SelectedIndex="{Binding SelectedIndex}">
                <TabControl.Resources>
                    <Style BasedOn="{StaticResource ChosenTab}" TargetType="TabItem"></Style>
                    
                </TabControl.Resources>
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <!--<TabItem Style="{StaticResource ChosenTab}" Header="{Binding Name}"></TabItem>-->

                        <TextBlock Text="{Binding Name}" Margin="0,2" FontSize="20" LineHeight="20" />
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <ListView ItemsSource="{Binding Data}" ItemContainerStyle="{StaticResource RowColourAlternate}" AlternationCount="2">
                            <ListView.View>

                                <GridView>
                                    
                                    <GridViewColumn Width="100" Header="Name" DisplayMemberBinding="{Binding Name}"/>
                                    <GridViewColumn Width="10" Header="CPR" DisplayMemberBinding="{Binding CPR}"/>
                                    <GridViewColumn Width="10" Header="Age" DisplayMemberBinding="{Binding Age}"/>
                                    <GridViewColumn Width="80" Header="Gender" DisplayMemberBinding="{Binding Gender}"/>
                                    <GridViewColumn Header="Specialty" DisplayMemberBinding="{Binding Specialty}"/>
                                    <GridViewColumn Header="Triage" DisplayMemberBinding="{Binding Triage}"/>
                                    <!--<GridViewColumn Header="ToHospital" DisplayMemberBinding="{Binding ToHospital}"/>
                                    <GridViewColumn Header="Note" DisplayMemberBinding="{Binding Age}"/>
                                    <GridViewColumn Header="AmbulanceNumber" DisplayMemberBinding="{Binding Age}"/>-->
                                </GridView>

                            </ListView.View>
                        </ListView>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </Grid>
    </Grid>
</UserControl>
